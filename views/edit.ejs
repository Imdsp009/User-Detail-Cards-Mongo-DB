<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Update User</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="/stylesheets/style.css">
</head>
<body>
  <div class="min-h-screen bg-zinc-900 text-white flex flex-col items-center px-5 py-10">
    <!-- Navigation -->
    <div class="w-full max-w-3xl mb-10">
      <a class="text-blue-500 hover:underline text-lg" href="/read">← Back to Users</a>
    </div>

    <!-- Page Title -->
    <h1 class="text-4xl font-bold tracking-tight text-center text-yellow-400 mb-8">Update User</h1>

    <!-- Update Form -->
    <form action="/update/<%= user._id %>" method="post" class="w-full max-w-3xl bg-zinc-800 p-8 rounded-lg shadow-xl">
      <!-- Name Input -->
      <div class="mb-6">
        <label for="name" class="block text-sm font-medium text-zinc-400 mb-2">Name</label>
        <input 
          id="name" 
          name="name" 
          type="text" 
          placeholder="Enter name" 
          value="<%= user.name %>"
          class="w-full px-4 py-2 text-zinc-200 bg-zinc-700 rounded-lg border border-zinc-600 focus:ring-2 focus:ring-blue-500 focus:outline-none"
          required
        />
      </div>

      <!-- Email Input -->
      <div class="mb-6">
        <label for="email" class="block text-sm font-medium text-zinc-400 mb-2">Email</label>
        <input 
          id="email" 
          name="email" 
          type="email" 
          placeholder="Enter email"
          value="<%= user.email %>"
          class="w-full px-4 py-2 text-zinc-200 bg-zinc-700 rounded-lg border border-zinc-600 focus:ring-2 focus:ring-blue-500 focus:outline-none"
          required
        />
      </div>

      <!-- Image URL Input -->
      <div class="mb-6">
        <label for="image" class="block text-sm font-medium text-zinc-400 mb-2">Image URL</label>
        <input 
          id="image" 
          name="image" 
          type="url" 
          placeholder="Enter image URL"
          value="<%= user.image %>"
          class="w-full px-4 py-2 text-zinc-200 bg-zinc-700 rounded-lg border border-zinc-600 focus:ring-2 focus:ring-blue-500 focus:outline-none"
        />
      </div>

      <!-- Submit Button -->
      <div class="flex justify-center">
        <button 
          type="submit" 
          class="px-6 py-2 bg-yellow-500 text-black font-semibold rounded-lg shadow hover:bg-yellow-600 transition duration-200"
        >
          Update
        </button>
      </div>
    </form>
  </div>
</body>
</html>
