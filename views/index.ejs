<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="/stylesheets/style.css">
  <title>Create User</title>
</head>
<body>
  <div class="min-h-screen bg-zinc-900 text-white flex items-center justify-center p-5">
    <div class="w-full max-w-lg bg-zinc-800 rounded-lg shadow-lg p-8">
      <!-- Navigation -->
      <div class="mb-6">
        <a class="text-blue-500 underline hover:text-blue-400" href="/read">&larr; Back to Users</a>
      </div>
      
      <!-- Title -->
      <h1 class="text-4xl font-bold mb-6 text-center text-yellow-400">Create User</h1>
      
      <!-- Form -->
      <form action="/create" method="post" class="flex flex-col gap-5">
        <!-- Name Field -->
        <div>
          <label for="name" class="block mb-1 text-sm font-medium">Name</label>
          <input 
            id="name" 
            class="w-full px-5 py-3 rounded-lg border-2 border-zinc-700 bg-transparent text-white focus:outline-none focus:ring-2 focus:ring-blue-500" 
            type="text" 
            placeholder="Enter your name" 
            name="name" 
            required>
        </div>
        
        <!-- Email Field -->
        <div>
          <label for="email" class="block mb-1 text-sm font-medium">Email</label>
          <input 
            id="email" 
            class="w-full px-5 py-3 rounded-lg border-2 border-zinc-700 bg-transparent text-white focus:outline-none focus:ring-2 focus:ring-blue-500" 
            type="email" 
            placeholder="Enter your email" 
            name="email" 
            required>
        </div>
        
        <!-- Image URL Field -->
        <div>
          <label for="image" class="block mb-1 text-sm font-medium">Image URL</label>
          <input 
            id="image" 
            class="w-full px-5 py-3 rounded-lg border-2 border-zinc-700 bg-transparent text-white focus:outline-none focus:ring-2 focus:ring-blue-500" 
            type="url" 
            placeholder="Enter image URL" 
            name="image" 
            required>
        </div>
        
        <!-- Submit Button -->
        <div class="text-center">
          <input 
            class="w-full px-5 py-3 rounded-lg bg-blue-500 text-white font-semibold hover:bg-blue-600 focus:ring-2 focus:ring-blue-400 transition duration-200 cursor-pointer" 
            type="submit" 
            value="Create">
        </div>
      </form>
    </div>
  </div>
</body>
</html>
